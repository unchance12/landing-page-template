## 🎯 Мета

Реалізувати верстку на **чистому HTML і CSS**, максимально наближену до наданого макету у Figma.

---

## 📁 Вихідні матеріали

- Макет у Figma: **https://surl.li/cyehky**
- Експортовані зображення (за потреби): `.png`, `.jpg`, `.svg`

---

## ⚙️ Вимоги до виконання

### **HTML**

1. Використовувати **семантичну розмітку** (`header`, `nav`, `main`, `section`, `article`, `footer` тощо).
2. Усі зображення повинні містити атрибути `alt`, `width`, `height`.
3. Списки (`ul`, `ol`, `dl`) застосовувати **відповідно до їх призначення**.
4. Усі посилання мають містити **атрибут `href`**.
5. Код повинен бути **структурованим** — дотримуйтесь відступів, вкладеності та додавайте за потреби коментарі.
6. Код має бути **відформатований за допомогою Prettier**.
7. Після завершення роботу слід **перевірити код у валідаторі** (W3C Validator).

### **CSS**

1. Стилі підключати **окремим файлом** (`style.css`) через тег `<link>`.
2. Використовувати **класи**, уникати інлайнових стилів.
3. Застосовувати **Flexbox** для побудови структури.
4. Код має бути **відформатований за допомогою Prettier**.
5. Після завершення роботу слід **перевірити CSS у валідаторі**.

---

## 💡 Підсумок

- Дотримуйтесь **відповідності верстки макету**.
- Забезпечте **чисту структуру коду** та **коректну валідацію**.
- Виконайте завдання відповідно до принципів **семантики та акуратності верстки**.
